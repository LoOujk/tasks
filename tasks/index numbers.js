document.addEventListener('DOMContentLoaded', function () {
    console.clear();
    // Получить число Pi из Math и округлить его до двух знаков после точки
        // console.log(Math.PI.toFixed(2));
    
    // Найти максимальное и минимальное значения из представленного ряда 10, 17, 5, 12, 15, 99, 1
        // valueMin = Math.min(10, 17, 5, 12, 15, 99, 1);
        // valueMax = Math.max(10, 17, 5, 12, 15, 99, 1)
        // console.log('Min = '+ valueMin + ' ; ' + 'Max = ' + valueMax);
    
    //С помощью Math.random:  ! получить случайное число и округлить его до двух цифр после запятой 
        // value = Math.random();
        // console.log(value.toFixed(2));
    
    //  получить случайное число от 0 до Х
        // x = 20;
        // value = Math.random() *  x;
        // console.log(value);
    // Получить число из строки '100$'
    let string = '100$';
    string = parseInt(string);
    console.log(string);

});
